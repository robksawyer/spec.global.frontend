/**
 * Publisher component to wrap all publisher specified stuff together. This component is divided to following logical components:
 *
 *  Controllers
 *  Models
 *
 * All of these are wrapped to 'frontend.publisher' angular module.
 */
(function() {
    'use strict';

    angular.module('frontend.publisher', []);
}());
